import 'package:flutter/material.dart';

class RoutineTab extends StatelessWidget {
  const RoutineTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Routine Tab'),
    );
  }
}
